// 앱 전체가 공유하는 타입. 한 곳에 모아두면 프런트엔드와(개념상) 백엔드가
// 같은 "형식" 개념을 쓰게 됩니다.
// TODO(i18n-comments): 학습용 한국어 주석은 임시. 최종적으로 영어 번역 또는 제거.

// 다운로드 형식은 딱 두 가지뿐. union 타입으로 못박아두면, 다른 문자열("wav" 등)을
// 실수로 넣었을 때 TypeScript가 컴파일 단계에서 잡아줍니다.
export type Format = "mp3" | "mp4";
